	    Если МодульОбъектаКлиент().ПолучитьЗначениеПараметраСБИС("ВключитьВО3") Тогда
			ДопПараметры = Новый Структура("СтруктураАккордеона", АккордеонЭДО);
			ПостроитьАккордеон(ДопПараметры);
		КонецЕсли;
