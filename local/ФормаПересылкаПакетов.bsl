
//УФ
&НаКлиенте
Процедура ПриОткрытии(Отказ)
	МодульОбъектаКлиент().ПолучитьЭлементФормыОбработки(ЭтаФорма,"Отправить").Заголовок="Отправить ("+строка(СписокДокументов.Количество())+")";
	ЭтаФорма.Заголовок = "Переслать для ознакомления";
КонецПроцедуры

